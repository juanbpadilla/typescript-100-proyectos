// const maxWidth = window.innerWidth * 0.9;  // 90% del ancho de la pantalla
// const maxHeight = window.innerHeight * 0.9; // 90% del alto de la pantalla

// const blockWidth = Math.floor(maxWidth / game.cols);
// const blockHeight = Math.floor(maxHeight / game.rows);

// // Usamos el más pequeño para que todo quepa
// const blockSize = Math.min(blockWidth, blockHeight);

// export const boardConfig = {
//   blockSize: 0,
//   rows: 9,
//   cols: 9,
//   mines: 9
// }

